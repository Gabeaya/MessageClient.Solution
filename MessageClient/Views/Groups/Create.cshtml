@model MessageClient.Models.Group

<h4>Add a new group</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name, new {@type="text", required = "required"})
  <input type="submit" value="Add new group"/>
}
<p>@Html.ActionLink("Show all groups", "Index")</p>